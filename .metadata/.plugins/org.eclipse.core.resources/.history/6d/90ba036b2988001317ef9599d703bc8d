function doClick(e) {
    alert($.label.text);
}

$.index.open();

var myBooks = Alloy.Collections.books;
var book = Alloy.createModel('books', { 
   id : '', 
   name: 'Charles Dickens',
   complete: 'false'
});
myBooks.add(book); 
book.save();